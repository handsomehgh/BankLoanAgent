# 项目亮点：用BERT分类器替代LLM做意图识别，提升银行智能客服稳定性与效率

## 一、背景与挑战

在银行智能客服项目中，我们基于LangGraph构建了多智能体架构，包含**LoanAdvisor**（贷款顾问）、**AfterLoan**（贷后管理）、**RiskAssessment**（风险评估）三个子Agent。每个Agent的执行流程为：
意图识别 → 参数提取 → 工具调用 → 回复生成

text

最初，第一轮意图识别完全由通用LLM（DeepSeek）完成，但很快暴露出生产环境不可接受的问题：

- **输出不稳定**：同样的用户输入可能得到不同的工具选择，有时甚至输出自然语言而不是工具名，导致整个流程中断。
- **延迟高**：每次LLM调用耗时500ms+，首字响应速度慢，客户体验差。
- **成本高**：每天数万次请求，LLM token消耗巨大，年成本可达数十万元。
- **领域混淆**：对于功能相近的金融工具（如“计算月供”和“计算总成本”），LLM分辨力不足，错误率居高不下。

### 为什么Supervisor继续用LLM，而子Agent改用BERT？

| 角色 | 标签空间 | 需求特点 | 决策 |
|------|---------|---------|------|
| Supervisor | 4~5类（路由到哪个Agent） | 决策简单、需要理解复杂职责边界 | 保留LLM |
| 子Agent第一轮 | 9~15类（具体工具或技能） | 要求高准确率、低延迟、强确定性 | 替换为BERT |

## 二、解决方案：用BERT替换子Agent第一轮LLM

我们决定**保持Supervisor用LLM做粗粒度路由**，但**将子Agent内部的第一轮意图识别替换为轻量级BERT分类器**。每个子Agent对应一个微调后的BERT模型，专门输出“工具名 / CLARIFY（意图模糊需反问） / DIRECT_REPLY（可直接回答）”。

### 架构变化（以LoanAdvisor为例）
原流程: LLM → 选工具（不稳定）→ 绑定参数 → 执行 → 回复
新流程: BERT分类器 → 选工具（稳定）→ LLM绑定参数 → 执行 → 回复

text

三个子Agent的标签空间：
- **LoanAdvisor**: 12类（10个工具/技能 + DIRECT_REPLY + CLARIFY）
- **AfterLoan**: 15类（13个工具/技能 + DIRECT_REPLY + CLARIFY）
- **RiskAssessment**: 9类（7个工具/技能 + DIRECT_REPLY + CLARIFY）

## 三、核心工作与亮点

### 1. 数据构造的“飞轮”设计

- **合成数据冷启动**：利用LLM批量生成各种对话场景下的用户提问，结合预定义的上下文模板（用户画像、对话历史、操作日志等），自动生成训练样本。通过**白名单过滤**LLM的不合法输出，确保标签干净。
- **真实交互数据闭环**：在现有系统运行过程中，记录LLM的每次决策标签，并根据用户反馈（是否纠错、工具是否成功执行）自动筛选高质量样本，持续补充训练集。
- **上下文构造**：BERT输入为 `[CLS] text_a [SEP] text_b [SEP]`，其中`text_b`为用户当前提问，`text_a`为动态拼接的对话上下文（用户画像、近期对话摘要、历史工具操作摘要、最近对话记录等），让模型理解多轮对话中的指代消解和话题切换。

### 2. 巧妙的数据清洗与质量保障

- **发现并解决上下文污染**：LLM在参数提取阶段会输出大量内部推理文本（如“根据您提供的信息，我来提取参数…”），这些内容会污染对话历史，导致BERT学习到错误模式。我们通过**在提示词中强制禁止输出推理过程**，并在消息记录时过滤推理关键词，彻底清理了训练数据。
- **标签边界严格化**：明确`CLARIFY`仅用于“连工具都无法确定”的极端模糊情况，绝不因缺少参数而误判。例如，用户说“利率”，即使参数不全，也选择`query_interest_rate`工具，后续由LLM追问参数。
- **类别不平衡处理**：针对低频但重要的类别（如`DIRECT_REPLY`、某些综合Skill），增加合成样本、调整损失函数类别权重，确保各类别的F1值均达标。

### 3. 模型选型与训练

- **基座模型**：`hfl/chinese-roberta-wwm-ext`，采用全词掩码训练，对金融术语理解精准，参数量仅110M，推理延迟<10ms（CPU）。
- **训练策略**：使用HuggingFace Trainer，宏平均F1作为优选指标，配合早停防止过拟合。
- **训练效率**：每个分类器仅需15-30分钟（GPU），数据量2k-3k条。

### 4. 线上集成与验证

- **影子模式部署**：BERT分类器上线后，先以影子模式运行，只记录预测结果不决策，与LLM进行对比验证两周，确认BERT准确率高于LLM后才全量切换。
- **可切换设计**：在`AgentNodeExecutor`中通过一个布尔开关`USE_BERT_CLASSIFIER`控制使用BERT还是LLM，便于回滚和A/B测试。
- **监控指标**：接入Prometheus，监控各类别的准确率、推理延迟、错误类型分布。

## 四、业务收益（可量化）

| 维度 | 优化前（LLM） | 优化后（BERT） | 提升 |
|------|--------------|--------------|------|
| 意图识别准确率 | ~93% | **98.5%+** | +5.5% |
| 第一轮决策延迟 | 500ms+ | **<10ms** | **50倍** |
| 年LLM token成本 | 约30万元 | 约20万元 | **节省33%** |
| 多轮对话意图保持率 | 较低 | 显著提升 | 减少“无意义反问” |
| 可审计性 | 差（黑盒） | 强（概率分布可追溯） | 满足银行合规要求 |

## 五、难点攻克与思考

1. **初期无标注数据怎么办？**  
   通过合成数据（LLM生成）构建冷启动数据集，再用真实交互的反馈信号（用户纠错、工具成功）自动筛选高质量样本，形成数据飞轮，摆脱了对人工标注的依赖。

2. **如何处理多轮对话中的上下文？**  
   上下文由用户画像、对话摘要、操作日志、最近对话四部分拼接而成，并限制长度避免超长截断。训练数据中特意加入了大量带上下文的样本，让BERT学会理解“那30年呢？”需要结合上一轮的“正在计算月供”来判断。

3. **如何确保三个Agent行为一致？**  
   三个BERT共享`DIRECT_REPLY`和`CLARIFY`的定义逻辑，但提示词基于各自业务场景定制，避免互相干扰。

## 六、个人能力体现

- **独立设计并落地**从数据生成、模型训练、到线上集成的完整AI产品化流程。
- **解决真实工程问题**（数据飞轮、上下文污染、类别不平衡），而不仅仅是跑通一个demo。
- **具备成本意识**：用轻量模型替代大模型，降低延迟和成本，追求生产级可靠性。
- **可复制的架构思维**：该方案可快速复制到其他智能客服或对话系统场景，替换不稳定且昂贵的LLM决策环节。

---

> **一句话总结**：我用三个BERT分类器，把子Agent的意图识别从“不稳定、慢、贵”变成了“稳定、快、省”，并搭建了完整的数据飞轮和监控体系，让AI真正落地在金融生产环境。