# 贷款领域 Embedding 模型微调项目总结

## 1. 项目背景与核心目标
- **业务痛点**：在银行智能客服的知识检索场景中，通用商业Embedding模型（如阿里云 `text-embedding-v4`）难以精准理解金融术语和区分细微语义差异，导致检索召回率存在瓶颈，影响最终的回答质量。
- **项目目标**：微调一个轻量级开源Embedding模型（`bge-small-zh-v1.5`），使其在银行信贷知识库上的检索准确率**超越**通用商业API，同时实现低成本、高效率的私有化部署，保障数据安全。

## 2. 训练数据体系设计（核心）
### 2.1 数据来源与规模
- **原始知识库**：390个金融贷款文档片段（FAQ、产品手册、流程指南、监管文件、术语表），共计约2000条有效训练数据。
- **元数据体系**：每个文档片段携带`product_type`（住房贷款/消费贷/经营贷/特色贷款/通用）和`topics`（利率/额度/申请条件/还款/逾期与征信/监管合规等）标签，基于关键词规则自动提取。

### 2.2 两类训练数据的构造策略

#### Query-Knowledge (QK) 数据
- **目标**：教会模型将用户的**口语化提问**映射到对应的**文档片段**。
- **正例构造**：
  - FAQ类文档：直接使用其自带的`question-answer`对，并通过LLM改写生成5个语义等价的口语化变体。
  - 产品手册/流程指南/监管文件/术语表：使用LLM基于文档内容生成5个不同角度的用户问题。
- **负例构造**：采用“困难负例 + 简单负例”混合策略。

#### Knowledge-Knowledge (KK) 数据
- **目标**：教会模型识别**不同文档片段之间的语义等价关系**。
- **正例构造**：
  - **规则优先**：利用`product_type`和`topics`标签，找到同一产品、同一主题的另一文档作为正例。
  - **语义兜底**：当规则匹配失败时，退化为用Teacher模型检索语义最相近的文档作为正例。
  - **LLM重写增强**：将文档改写为“客户经理话术”、“风险提示短信”、“合同条款说明”三种风格，生成3个语义等价的正例。
- **负例构造**：同样采用“困难负例 + 简单负例”混合策略。

### 2.3 困难负例挖掘（核心创新点）
这是本项目中提升模型区分能力的关键技术。

- **多Teacher模型共识检索**：采用`bge-m3`、`text2vec-large-chinese`、`m3e-large`三个异构强模型，对每个查询分别检索Top-200文档，取多模型高分交集作为候选。
- **元数据规则筛选**：对候选文档进行业务规则过滤，保留两类困难负例：
  - **同产品、相邻主题**：如查询“住房贷款利率”，困难负例为同一产品的“还款方式”文档。
  - **同主题、不同产品**：如查询“住房贷款利率”，困难负例为“消费贷利率”文档。
- **语义去重**：通过Jaccard相似度（阈值0.8）过滤高度重复文档，避免假正例混入负例池。

### 2.4 数据质量控制
- **模板占位符过滤**：自动过滤分块过程中产生的无意义占位符文本。
- **负例与正例去重**：确保负例列表中不包含与正例相同或高度重复的文档。
- **评估数据分离**：独立生成QK和KK评估集（各50条），仅包含正例对，不参与训练，保证评估客观性。

## 3. 训练配置与策略
- **基座模型**：`BAAI/bge-small-zh-v1.5`（33M参数，384维）
- **损失函数**：`MultipleNegativesRankingLoss`，在一个批次内自动将其他样本的正例和困难负例作为负样本，最大化训练信号利用效率。
- **训练环境**：AutoDL云GPU服务器，Python 3.12，`sentence-transformers 2.7.0`
- **关键超参数**：学习率 `2e-5`，batch size `16`，epochs `5`，warmup steps 占总步数 10%
- **评估策略**：使用`InformationRetrievalEvaluator`，每个epoch计算Recall@k、MRR、NDCG等指标，配合自定义早停回调（patience=3）选择最佳模型。

## 4. 效果评估与量化成果
### 4.1 测试条件
- **独立测试集**：100条LLM生成的、完全自包含的用户口语化问题。
- **检索库**：114个金融贷款知识文档片段。
- **对比基线**：阿里云商业模型 `text-embedding-v4`（1024维）。

### 4.2 核心指标

| 模型 | Recall@5 | MRR@5 | 命中数(100) |
| :--- | :--- | :--- | :--- |
| 阿里云 text-embedding-v4 | 85.00% | 0.7653 | 85 |
| **自研微调 bge-small-loan** | **86.00%** | **0.7753** | **86** |

### 4.3 结果解读
- **在强基线上实现超越**：商业模型已达85%高召回率，微调模型仍提升1个百分点。
- **性能与成本双赢**：33M参数轻量模型，以**更低推理成本和更快速度**，**持平甚至略超**商业API，数据完全私有化。

## 5. 部署方案
- **服务化部署**：使用FastAPI + SentenceTransformer搭建Embedding推理服务，暴露OpenAI兼容的`/v1/embeddings`接口。
- **远程访问**：通过SSH隧道或HuggingFace Spaces实现本地开发环境与远程GPU服务器的安全连接。
- **向量数据库集成**：与Milvus深度集成，支持稠密向量+稀疏向量混合检索，所有向量维度统一为384。

## 6. 项目价值与面试亮点
- **技术闭环**：独立完成“数据构造 → 模型微调 → 服务部署 → 效果评估”全流程。
- **成本与安全优势**：“零成本”私有模型替代昂贵商业API，杜绝金融数据外泄风险。
- **深度思考**：能清晰阐述InfoNCE损失、困难负例挖掘、Teacher-Student模型协同、对比学习与交叉熵的数学联系。
- **工程方法论**：沉淀了一套可复用的领域Embedding微调方法论，可快速迁移到其他业务场景。

## 7. 展望与优化方向
- 引入用户反馈闭环，持续迭代困难负例。
- 扩展多模态文档解析能力，处理表格、扫描件等复杂文档。
- 融合BM25稀疏检索，实现混合检索进一步提升召回。
- 尝试Matryoshka Embedding，支持动态调整向量维度。